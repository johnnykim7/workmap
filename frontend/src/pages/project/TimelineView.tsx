import { StubPage } from '@/components/common/stub-page';
// 타임라인/로드맵 탭 — start~due 막대 (P2).
export function TimelineView() {
  return <StubPage title="타임라인" desc="start~due 막대 로드맵" sprint="Phase2" />;
}
