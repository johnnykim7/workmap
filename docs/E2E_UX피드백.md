# WorkMap E2E UX 피드백 누적

> **목적**: 사용자가 직접 운영 화면을 E2E로 돌면서 발견한 불편·디자인·UX·버그를 한곳에 누적.
> **규칙**: 던진 항목은 **즉시 수정하지 않는다.** 여기 기록만 하고, 사용자가 "수정하자" 하면 모아서 일괄 수정.
> 시작: 2026-06-27 | 환경: 운영 (FE nginx `/home/therecommerce/workmap/frontend/`, BE 8186)

---

## 작성 규칙
- 한 항목 = 한 행. 사용자가 말한 그대로 + (필요 시) 화면/기능ID 보강.
- **상태**: 🆕 신규 / 🔧 수정중 / ✅ 수정완료 / ❌ 보류·기각
- **유형**: `디자인` (색/간격/정렬/톤) · `UX` (흐름/문구/동작/피드백) · `버그` (동작 안 함/에러) · `기능` (없는 기능 요청)
- **우선순위**: H(즉시) / M(보통) / L(나중)

---

## 피드백 목록

| # | 상태 | 화면 / 기능ID | 유형 | 우선 | 내용 (사용자 발언) | 수정 방향 (작성자 메모) |
|---|------|--------------|------|------|--------------------|------------------------|
| 1 | ✅ | 로그인 / WMP-AUTH-001 | UX | M | "회원가입은 어디서 하나요?" — 로그인 화면에 가입 진입점도, 가입 불가 안내도 없어 혼란 | **[수정완료 2026-06-28]** [LoginPage.tsx](frontend/src/pages/LoginPage.tsx) 로그인 폼 아래에 안내 문구 추가 — "계정이 없으신가요? 관리자에게 계정 생성을 요청하세요." self-signup 부재(WMP-AUTH-004 관리자 초대 모델) 명시. 가입 화면 신설 아님. tsc+build PASS, 운영 배포·번들 일치 확인. |
| 2 | ✅ | 전역 구조 라인 톤 통일 / main.css (회사홈·테이블·LNB·GNB) | 디자인 | M | "라인이 진한 건 연하게는 맞는데 너무 연하다 / 테이블 안쪽은 진하고 LNB 우측·GNB 하단은 너무 연해서 안 보임 → 같은 톤으로 통일+약간 진하게" | **[수정완료 2026-06-28 — 최종]** **근본 원인 = 라인마다 색 소스가 제각각이었음**(단일 토큰 아님): ① **테이블 행** = ds-ui `TableRow`가 `border-b` **색 미지정**([index.js:2262]) → `currentColor`(글자색≈검정)로 진함 ② **LNB 우측·GNB 하단** = AdminShell `admin-shell-header` `border-b`·sidebar `border-r`도 **색 미지정**([index.js:5617] 등) ③ **카드/입력 등** = `border-border`(토큰) → 연함. `--sidebar-border` 토큰은 floating variant 전용이라 기본 사이드바엔 무효(헛다리). ④ **divide-y 목록 구분선**(받은함·회사홈 미배정) = Tailwind v4에서 `divide-border` 유틸 **미생성** + `divide-y`가 border-color 안 줌 → 검정. ⑤ **ds-ui Table 행 구분선이 일부 화면에서 아예 증발**(목록탭·검색) = 번들에 Tailwind v4.2(ds-ui)+v4.3(앱) 혼재 → `border-b`의 `border-bottom-style:var(--tw-border-style)`이 비어 선 안 그려짐. **최종 해결**: (a) 전역 [main.css](frontend/src/styles/main.css) — `--border` `oklch(94%)`→**`oklch(89%)`** + 색 미지정 소스에 `border-color:var(--border)`+`border-style` 강제(`[data-slot=table-row/header/admin-shell-header/sidebar]`·`.divide-y>:not(:last-child)`)로 LNB/GNB/divide/카드 통일. (b) **ds-ui Table 행은 전역 CSS가 안 닿아 컴포넌트 직접 지정** — [WorkItemTable.tsx](frontend/src/features/workitem/components/WorkItemTable.tsx)를 `rounded-lg border` 외곽 박스로 감싸고 각 `TableRow`에 `className="border-b border-border"` 부여(관리자 테이블이 보였던 구조 복제). → **전 구조 라인 통일·전 화면 구분선 표시 확인.** tsc+build PASS, 운영 배포·사용자 육안 확인(:3186, `index-CPK8bsUb`). 상세 함정은 메모리 `workmap-border-tone`. **[헤맨 기록]** ⓐ "카드 외곽 안 보임" 오해→토큰 88→82% 올렸다 역효과 원복 ⓑ `--sidebar-border`는 floating 전용 헛다리 ⓒ divide-y `~` 결합자로 "첫 줄만 검정" 버그→`:not(:last-child)` 정정 ⓓ table 행은 `--tw-border-style` 증발로 전역 CSS 무효→컴포넌트 직접 지정으로 최종 해결. 색 소스 5종 흩어진 게 근본. |
| 3 | ❌ | 회사 홈 / 공통 레이아웃 | 디자인 | M | (스샷 관찰) 우상단 "관리자" 버튼이 가로 폭에 잘려 "관" 한 글자만 보임 / (사용자 정정) "관 한 글자만 보이는데 그게 컨셉 아닌가요?" | **[기각 — 버그 아님, 코드 실측]** 사용자 정정이 맞음. 그 "관"은 잘린 버튼이 아니라 **계정 아바타의 이름 이니셜**(`AvatarFallback`=`user.name[0]`, [AppShell.tsx:159](frontend/src/layout/AppShell.tsx#L159)). 로그인명이 "관리자"라 첫 글자 "관" 표시 = Gmail·Jira·Slack과 동일한 **의도된 아바타 컨셉**. HeaderActions에 "관리자" 텍스트 버튼은 처음부터 없음(git `-S "관리자"` 히스토리 0건). 헤더 우측 = [만들기] + 아바타뿐. → 고칠 것 없음. (작성자 오인: 스샷의 원형 아바타를 잘린 버튼으로 잘못 읽음.) |
| 4 | ✅ | 업무 상세 / WMP-WI-004 | UX | M | "상세 화면 들어갔을 때 뒤로 가기 같은 버튼이 없음" | **[수정완료 2026-06-28]** [DetailHeader.tsx](frontend/src/features/workitem/components/DetailHeader.tsx) 상단 좌측에 ← 뒤로 가기(`navigate(-1)`) 추가. **풀페이지 진입(라우트)일 때만 노출** — `showBack` prop을 WorkItemDetailPanel `stacked`로 제어([WorkItemDetailPanel.tsx](frontend/src/features/workitem/components/WorkItemDetailPanel.tsx), 분할뷰는 같은 화면 패널이라 숨김). 기존 위/아래 화살표(항목 이동)와 별개. tsc+build PASS, 운영 배포 확인. |
| 5 | ✅ | 업무 상세 / WMP-WI-008 (#19와 동일 원인) | 버그 | M | "세부 사항에서 시작일/기한 우측 디자인 정렬이 깨짐" | **[수정완료 2026-06-28 — #19와 일괄]** (유형 정정: 디자인→버그. 색/톤이 아니라 레이아웃 깨짐.) DetailSidePanel 시작일/기한 DatePicker가 `w-[240px]` 고정폭이라 패널 폭 넘어 잘림 → `className="w-full"` 주입. #19와 같은 근본·일괄 처리. 운영 배포 확인. |
| 6 | ❌ | 업무 상세 / WMP-WI-008 (DatePicker) | UX | M | "달력을 클릭하면 선택되고 사라지는 게 맞을지 검토 필요. 현재는 사라지지 않고 여백을 클릭해야 사라짐" | **[코드 실측됨 → ds-ui 백로그 분류, WorkMap 미수정]** ds-ui `DatePicker`(`gs`)가 `Popover`+`Calendar mode=single`인데 **open 상태를 내부 통제 안 함** — `onSelect`는 소비앱 `onChange`로만 연결되고 선택 후 close 로직 없음(Popover uncontrolled→바깥 클릭으로만 닫힘). 사용자 관찰 정확. 단일선택 즉시닫힘은 통상 UX라 타당하나, **value/onChange만으론 소비앱에서 못 고침** — DatePicker를 ds-ui 안 쓰고 자체 조립해야 하는데 그건 ds-ui 사용 규칙·일관성에 역행. → #10·#19와 같은 "ds-ui 레벨로 올리기" 트랙. ds-ui DatePicker에 onSelect→close(단일)·범위 예외를 넣어 전 프로젝트 공유. WorkMap 단독 수정 대상 아님. |
| 7 | ✅ | 전역 [만들기] / **WMP-WI-001** | **버그(기능누락)** | **H** | "업무 만들기 누르면 'Sprint3에서 구현됩니다' stub가 뜸. Sprint3은 이미 구현 완료 아닌가?" | **[수정완료 2026-06-27]** [create-modal.tsx](frontend/src/components/common/create-modal.tsx) stub → 실제 생성 폼 교체. `POST /work-items`(BE CreateRequest 계약) 연결: 필수=프로젝트·유형·요약 / 선택=설명·담당자·우선순위·라벨·Epic(§9.4 "만들기는 가볍게"). 유형은 프로젝트 템플릿 issueTypeCodes로 좁힘, 담당자=프로젝트 멤버, Epic=프로젝트 내 EPIC 항목. `[ ] 다른 항목 만들기` 연속 생성. `features/workitem/api.ts`에 `create` + `hooks.ts`에 `useCreateWorkItem` 추가. ds-ui Dialog+RHF+Zod. tsc+vite build 통과, api 계약 테스트 8/8 PASS. **함께 처리된 FE 미연결 3종**: WS-001(워크스페이스 생성/수정)·WS-004(프로젝트 수정/보관)·WS-006(가시성 변경) — ProjectsPage 헤더/카드 메뉴·SummaryView 설정 버튼·ProjectSettingsDialog로 연결. |
| 8 | ✅ | 업무 상세 헤더 / (53종 밖) | UX | L | (스캔 중 발견) 상세 헤더의 "구독(워치)" 버튼 클릭 시 "워치 기능은 준비 중입니다" 토스트만 — 동작 없음 | **[수정완료 2026-06-28]** 워치/구독 버튼 숨김([DetailHeader.tsx](frontend/src/features/workitem/components/DetailHeader.tsx)). T1-1 53종 밖 + 동작 없음(준비 중 토스트만)이라 미완성 인상 방지. 정식 추가는 Phase 재검토 대상으로 보류. tsc+build PASS, 운영 배포 확인. |
| 9 | ✅ | 검색 인풋 전반 / ds-ui SearchInput | 버그(배포) | M | "인풋박스에 아이콘이 있는 경우 글자와 겹치는 케이스가 있음" (스샷: 프로젝트 필터바 "프로젝트명·키 검색" — 돋보기 아이콘과 placeholder 겹침) | **[수정완료 2026-06-28 — 재배포로 해소]** 코드 버그 아님(ds-ui SearchInput은 `left-3`+`pl-9`로 겹침방지 정상). 원인은 운영 구버전 CSS. 2026-06-28 헤더정리 배포 시 최신 dist CSS 반영 → 운영 서버 `index-BlpxR6wH.css`(134KB)에 `.pl-9`·`--spacing` 존재 확인(ssh grep). SearchInput 쓰는 전 화면 공통 해소. (구 진단: 운영 `index-C7wRzaPA.css` 58KB엔 `.pl-9`/`--spacing` 부재였음.) |
| 10 | 🆕 | 관리자 테이블 전반 / ds-ui Table (스샷=측정단위 WMP-ADM-001) | 기능 | L | "이 테이블이 ds-ui 컴포넌트인가? 페이징/정렬/드래그 리사이즈/인라인 편집이 있나?" | **[코드 실측됨]** ① 테이블=**ds-ui 정식** `Table/TableHeader/TableBody/TableRow/TableHead/TableCell` ([MeasureUnitsPage.tsx:6-7](frontend/src/pages/admin/MeasureUnitsPage.tsx#L6), 임시 `<table>` 아님). ② 페이징=**있음**, 단 ds-ui `Pagination`이 아니라 Button 2개 자체 prev/next([130-140줄](frontend/src/pages/admin/MeasureUnitsPage.tsx#L130), 번호점프 없음·`totalPages>1`일 때만 노출). ③ 컬럼정렬(헤더 클릭 sort)=**없음** ("정렬" 컬럼은 데이터 `sortOrder` 표시값일 뿐). ④ 컬럼 드래그 리사이즈=**없음**(폭 `w-20/w-28` 고정, ds-ui Table 미지원). ⑤ 인라인 편집=**없음**(행 [수정]→`MeasureUnitDialog` 모달). **결론(사용자 방향 확정): 이 기능들은 WorkMap에서 화면별로 자체 구현하지 않는다 — 필요하면 ds-ui Table에 sort/resize/inline-edit/Pagination을 넣고 전 프로젝트가 공유한다(조직 공통 컴포넌트 재사용 원칙). WorkMap 단독 수정 대상 아님 → ds-ui 개선 백로그로 분리.** 우선 L. |
| 11 | 🆕 | 타임라인 / WMP-VIEW (TimelineChart) | 디자인 | M | "타임라인을 라이브러리를 사용한 것인가? 헤더가 허접하네요" (스샷: 좌측 '항목' + 우측 '7월' 한 칸만 있는 시간축 헤더) | **[코드 실측됨]** ① **라이브러리 미사용 — 100% 자체 구현.** 간트 라이브러리(frappe-gantt/react-calendar-timeline/vis 등) import 0건. [TimelineChart.tsx](frontend/src/features/view/components/TimelineChart.tsx)가 div+`absolute left/width %` 막대를 직접 그리고, 월 눈금도 자체 `monthTicks()`([timeline-util.ts](frontend/src/features/view/timeline-util.ts))로 계산. ② **헤더 빈약 원인**: 헤더([37-47줄](frontend/src/features/view/components/TimelineChart.tsx#L37))가 **월 시작점에만 라벨+세로선 1개**를 찍음 → 데이터가 한 달에 몰리면 눈금 딱 1개라 휑함. 라벨은 세로선 우측 `pl-1`로 흘려 붙고(셀 중앙정렬·연도·격자배경 없음), 본문 행엔 세로 격자선이 없어 막대 위치를 시간축과 대조 불가. 간트 라이브러리 기본 제공(시간축 격자·주말 음영·today 라인·줌)이 전부 부재. **수정 방향(사용자 결정 2026-06-27)**: 지금 손대지 않고 **추후 필요해지면 검증된 타임라인/간트 라이브러리를 도입**하는 것으로 기록(자체 헤더 보강은 추진 안 함). 도입 시 ds-ui 톤·색 토큰에 맞춰 커스텀. 우선 M(보류성). |
| 23 | ✅ | 백로그 행 우측 영역 / BacklogRow §6.1 | 디자인 | M | "우측 보면 Epic 지정 등 뭔가 난잡함" (백로그/스프린트 행 우측에 Epic드롭다운·날짜·SP·우선순위·상태가 빽빽) | **[수정완료 2026-06-29 — 사용자 결정 "그대로 두고 정렬만"]** 우측 메타 6종(Epic·날짜·SP·우선순위·상태·담당자)을 **폭 고정 슬롯**(`w-36/w-14/w-7/w-6/w-20/w-6`, 각 justify)으로 정렬 → 행마다 칼럼처럼 위치 일치, 빽빽·들쭉날쭉 완화([BacklogRow.tsx](frontend/src/features/agile/components/BacklogRow.tsx)). Epic placeholder·색은 유지(사용자 선택, hover 숨김 안 함). **부수 발견·수정 2건**: ① ds-ui `SelectTrigger` base에 `[&>span]:line-clamp-1`(직계 span 세로 box 강제)이 있어 Epic placeholder·**선택된 EpicChip(span 버전) 모두 2줄로 깨짐** → 둘 다 `<div>`로 감싸 회피, 한 줄 복구. ② justify만으론 칩 가변폭 탓에 시작/끝이 들쭉날쭉(우측이든 좌측이든) → **셀 고정폭+셀내 좌측정렬 통일**(Epic `w-36`+truncate, 날짜 `w-16 justify-start`, SP/우선순위/상태/담당자 center 유지)로 칼럼 정렬. tsc+build PASS, 운영 배포·사용자 육안 확인. | #18(Epic 인라인 연결) 추가로 [BacklogRow.tsx](frontend/src/features/agile/components/BacklogRow.tsx) 우측에 요소 6개 일렬: Epic드롭다운([54-77줄](frontend/src/features/agile/components/BacklogRow.tsx#L54))·dueDate([82](frontend/src/features/agile/components/BacklogRow.tsx#L82))·SP([88](frontend/src/features/agile/components/BacklogRow.tsx#L88))·우선순위([91](frontend/src/features/agile/components/BacklogRow.tsx#L91))·상태([92](frontend/src/features/agile/components/BacklogRow.tsx#L92))·담당자아바타([93](frontend/src/features/agile/components/BacklogRow.tsx#L93)). **난잡 원인 3**: ① **"Epic 지정" placeholder가 미지정 행마다 상시 노출**([66-68줄](frontend/src/features/agile/components/BacklogRow.tsx#L66)) — Epic 없는 행 전부에 "Epic 지정 ▾"가 떠 시각 노이즈. Jira는 미지정 시 비우거나 hover에만 노출. ② **요소 6개가 정렬 기준 없이 빽빽** — 폭 고정/우측 정렬 칼럼 없이 `gap-2`로 흐름배치라 행마다 위치 들쭉날쭉. ③ **색 과다** — 보라 EpicChip([badges.tsx:38])+파랑 StatusBadge+노랑 우선순위(`Equal text-amber-500` [badges.tsx:73])가 한 행에 몰림(CLAUDE.md 색 절제 위배). (스샷의 회색 "—"는 빈 버튼 아님 = MEDIUM 우선순위 `Equal` 아이콘.) **수정 방향**: Epic placeholder를 hover/포커스 시에만(미지정은 평소 숨김), 우측 메타를 폭 고정 칼럼으로 정렬(SP·우선순위·상태·담당자 고정 슬롯), 색은 상태/막힘 신호로만 축소. #2(카드 톤)·#3(헤더 버튼)과 함께 디자인 정리 묶음. 우선 M. |
| 22 | ✅ | 프로젝트 상단 헤더 / ProjectLayout (Jira 스페이스 헤더 비교) | UX(디자인) | M | "Jira는 최상단에 '스페이스'라는 글자, 우리는 '프로젝트 > 코드' 이렇게 보여짐. 둘 다 크게 의미 없는 듯 보임." | **[수정완료 2026-06-28]** 브레드크럼(`프로젝트 > KEY`) + 제목 h1 2줄을 **둘 다 제거**([ProjectLayout.tsx](frontend/src/layout/ProjectLayout.tsx)) → 탭부터 시작. 프로젝트명은 글로벌 헤더 좌측 타이틀(`HeaderTitle`)로 이동, **클릭=프로젝트 목록 이동**([AppShell.tsx](frontend/src/layout/AppShell.tsx))으로 브레드크럼 회귀 기능 흡수. (LNB '프로젝트' 행은 children 보유라 클릭=toggle만 되어 목록 진입 불가 — ds-ui `AdminShell` 핸들러 실측: children 있으면 toggle, 없으면 navigate. 그래서 회귀 경로를 헤더 제목 링크로 둠.) tsc+build PASS, 운영 배포·번들 일치 확인. (구 진단: 우리 헤더 3단 브레드크럼+제목+탭이 정보 중복, LNB 진입감 확보된 지금 본문 브레드크럼은 잉여.) |
| 21 | ✅ | 상단 검색바 vs LNB '검색' 중복 / WMP-§13.9·AppShell | UX(중복) | M | "Jira는 상단 검색을 클릭하면 그 자리 드롭다운이 뜸. 우리는 상단 검색을 클릭하면 좌측 LNB 선택(=검색 화면)으로 이동함. 그렇다면 굳이 상단 검색 메뉴가 필요할까?" | **[수정완료 2026-06-28 — (A)안 채택]** 상단 헤더 검색바 제거([AppShell.tsx](frontend/src/layout/AppShell.tsx) HeaderActions) → 그 자리에 `HeaderTitle`(현재 화면명/프로젝트명). LNB '검색' 1급 메뉴만 유지(발견성 충분). 중복 직접 해소·소규모. tsc+build PASS, 운영 배포·번들 일치 확인. (구 진단: 둘 다 `navigate('/search')`로 완전 중복.) |
| 20 | ✅ | 프로젝트 노출 탭 반영 / WMP-WS-004·ProjectLayout | **버그** | **H** | "탭을 노출했음에도 탭에 나타나지 않음. 로그아웃 및 리프레시해도 안 나타남" (설정에서 목록·보드·캘린더·승인 체크 저장 → 화면은 요약·타임라인·보고서만) | **[코드 실측됨 — 사용자 보고 정확]** 저장은 정상(ProjectSettingsDialog가 `activeTabs` 폼→PATCH 전송 [47·91줄](frontend/src/features/projects/components/ProjectSettingsDialog.tsx#L47), Project 타입에 `activeTabs` 존재 [domain.ts:157], DB active_tabs JSONB 저장 운영검증됨). **버그는 렌더가 저장값을 무시**: [ProjectLayout.tsx:18](frontend/src/layout/ProjectLayout.tsx#L18)이 탭을 `project.activeTabs`가 아니라 **템플릿 유형 프리셋** `PROJECT_TAB_PRESET[templateType(project.templateId)]`으로 정함. WMS=PLAN형 → 프리셋 `['summary','timeline','reports']`([domain.ts:99]) = 스샷 "요약·타임라인·보고서" 정확 일치. 사용자가 켠 목록·보드·캘린더·승인은 DB에 있어도 화면이 코드 상수만 봐서 안 뜸 → 로그아웃·리프레시 무효(서버값 미참조). 주석([17줄](frontend/src/layout/ProjectLayout.tsx#L17)) "Phase1 고정"이라 의도된 미연결이나, 설정 화면이 탭 편집을 제공하는 이상 명백한 버그. **수정 방향(한 줄)**: `const tabs = project?.activeTabs?.length ? project.activeTabs : PROJECT_TAB_PRESET[templateType(project.templateId)]` — 저장된 activeTabs 우선, 없으면 프리셋 폴백. (ProjectTab 타입 캐스팅·#14 BE/FE defaultTabs 불일치도 함께 점검 권장.) 우선 H. |
| 19 | ✅ | DatePicker 전반 / ds-ui (스샷=스프린트 만들기 WMP-AGL) | 버그 | M | "디자인 깨짐" (시작일/종료일 DatePicker 우측이 패널 폭 넘어 잘림) | **[수정완료 2026-06-28 — 즉효안 채택]** ds-ui DatePicker `w-[240px]` 고정폭이 좁은 컨테이너서 잘리던 것 → **사용처 7곳(4파일) 전부 `className="w-full"` 주입**(twMerge로 덮임): CreateSprintDialog 2 · ProjectSettingsDialog 2 · DetailSidePanel 2(=#5) · FieldVerifications 1. (ProjectCreateWizard는 #12에서 이미 처리됨.) 근본(ds-ui 기본폭)은 별도 ds-ui 백로그. tsc+build PASS, 운영 배포·번들 일치(`index-mknKspE_.js`) 확인. (구 진단: 5개 사용처 전부 w-full 미전달.) |
| 18 | ✅ | 에픽 생성 진입점 / 백로그 WMP-AGL·WMP-WI-001 | 버그(기능누락) | M | "에픽 만들기 화면은 없나요?" / "에픽 생성 개념 자체가 없다는 건가요?" | **[수정완료 2026-06-28 — CR-022]** **방향 전환: "Epic 펼침 트리" 폐기 → Jira 방식(평면+칩+필터+인라인).** 원전(Mike Cohn, 3-vote 확정: "epic=a large user story", 느슨한 그룹핑)·우리 코드 실측(epic_id FK없는 nullable, Sub-task만 강제계층)·Jira 캡처(평면+소속칩+필터) 교차검증 결과, 하이라키 트리는 원전·Jira·우리구현 모두와 어긋나 폐기. 구현: ① Epic 소속 칩(EpicChip, 각 행에 "어느 Epic 소속") ② Epic 필터(백로그 상단 드롭다운, 특정 Epic 소속만) ③ 인라인 생성(InlineCreateRow, 각 구역 하단 +만들기→제목 Enter, 스프린트 구역=sprintId·Epic필터=epicId 프리필). 신규 BE 0(CreateRequest.sprintId/epicId 이미 존재). epic-filter.test.ts 4/4 PASS, tsc+build 통과. 설계 T3-3 §6.1 정정. **운영 배포·화면 E2E는 미수행(다음).** ▼ 이하 조사 경위 ▼ **[FE+BE+설계 실측됨]** **⭐ 먼저 명확히: 에픽 생성 개념·경로는 실재하고 동작함(없는 게 아님).** BE `WorkItemService.create`가 `issueType=EPIC`를 받아 저장([WorkItemService.java:72·97](backend/src/main/java/com/therecommerce/workmap/workitem/service/WorkItemService.java#L72)) + 에픽 진행률 자동 집계 `recomputeEpicProgress`([434-450줄](backend/src/main/java/com/therecommerce/workmap/workitem/service/WorkItemService.java#L434))로 에픽을 1급 개념으로 다룸. FE 만들기 모달도 EPIC을 유형으로 노출([create-modal.tsx:81-83](frontend/src/components/common/create-modal.tsx#L81))·그대로 POST([103줄](frontend/src/components/common/create-modal.tsx#L103))·제목 "에픽 만들기"로 전환 → **DEV/PLAN 프로젝트에선 모달로 에픽 실제 생성 가능.** (OPS 템플릿엔 EPIC 미포함이라 그 프로젝트만 모달에 에픽 안 뜸 — 설계 의도.) **[FE+설계 실측됨]** ① **전용 "에픽 만들기" 화면은 설계상 불필요** — T3-3 §9.4([T3-3:223](docs/T3-3_화면_구조.md#L223)) "유형이 뭐든 동일한 모달 하나, 제목만 '에픽 만들기'로". 즉 공용 만들기 모달에서 유형=에픽이 정답. ② **모달에서 에픽 생성은 조건부 가능** — 유형 목록이 프로젝트 템플릿 `issueTypeCodes`로 좁혀짐([create-modal.tsx:81-84](frontend/src/components/common/create-modal.tsx#L81)). EPIC 포함 템플릿=DEV·PLAN, OPS엔 없음([domain.ts:187-189](frontend/src/types/domain.ts#L187)) → DEV/PLAN 프로젝트만 모달로 에픽 생성 가능. 제목도 "에픽 만들기"로 전환됨([create-modal.tsx:131](frontend/src/components/common/create-modal.tsx#L131)). ③ **진짜 빈틈 = 백로그 화면 내 Epic 세부기능 2개 미구현(화면 통째 누락 아님 — 정정)** — 백로그 화면([BacklogView.tsx](frontend/src/pages/project/BacklogView.tsx))은 **존재·동작함**(스프린트 구역+백로그 구역, dnd-kit 드래그 이동, 스프린트 생성/시작/완료 FSM). 다만 (a) **Epic 펼침 트리 없음** — 항목을 `SprintSection`에 평면 리스트로만 넣고 에픽 그룹핑 로직 없음([BacklogRow.tsx](frontend/src/features/agile/components/BacklogRow.tsx)는 한 줄 렌더만). 설계 [T3-3:141](docs/T3-3_화면_구조.md#L141)·[293](docs/T3-3_화면_구조.md#L293) 요구분 미구현. (b) **인라인 생성 없음** — 백로그엔 "스프린트 만들기" 버튼만([BacklogView.tsx:76](frontend/src/pages/project/BacklogView.tsx#L76)), 항목/에픽을 그 자리서 만드는 UI 없음. 설계 `BacklogRow(Epic 트리/인라인 생성)` 중 인라인 생성 미구현. **(자기정정: 최초 grep만으로 "통째 미구현"이라 단정했으나 화면 직접 읽으니 화면·핵심은 동작, 빠진 건 Epic 트리/인라인 생성 2개 세부.)** **수정 방향**: 단기—만들기 모달이 에픽 생성 정답임을 UX로 명확히(DEV/PLAN 유형에 에픽 노출 확인). 본격—백로그에 Epic 펼침 트리 + 인라인 생성 추가(설계 내 미구현 세부, 계획된 화면 실행이라 CR 불요·진척 기록). 우선 M. |
| 17 | ✅ | 업무 만들기 모달·상세 "설명"란 리치에디터 / WMP-WI-001 | 기능 | M | "업무 만들기에서 설명란이 에디터인 경우 좀 과할까요?" → (의도 재확인) "에디터를 쓰자는 의미. 이미지도 들어가야 함" | **[설계 캐스케이드 완료 — CR-024, 2026-06-28]** **직전 "에디터화 과함" 결론 철회.** 사용자 의도는 "에디터 도입"이 목적이었고, 실측으로 두 사실 확정: ① 만들기 모달 설명 = 상세 설명 = **같은 1필드(`description`)** → 들어갈 곳은 모달+상세 둘뿐(다른 곳 없음). ② T3-3 설계는 **이미 "리치 텍스트/리치 에디터(이미지 포함)"로 정의**돼 있었음(§9.3 232줄·§9.4 256줄·320줄 RichEditor) → 구현이 설계보다 낮게 내려간 상태(plain Textarea)였을 뿐 "과함"이 아니라 "미달"이었음. **결정**: Tiptap 리치 에디터(조직 일관성 — axopm IssueEditor 동형) 공용 컴포넌트로 모달·상세 동일 적용. 이미지=별도 파일 업로드(`POST /files/upload`, 로컬 디스크 저장, URL만 description HTML에 삽입). 저장형식=HTML 문자열(`description` text 컬럼 무변경). T3-2(F3 파일업로드)·T3-3(§9.3·§9.4) 갱신 + CR-024 기록 완료. **구현 대기(사용자 승인 후 착수)**. 우선 M. |
| 16 | ✅ | 네비게이션 IA 전반 / 진입감(워크스페이스+프로젝트)·LNB (T3-3 §9.1) | UX(구조) | **H** | "워크스페이스도, 프로젝트도 '뭔가 진입했다'는 개념이 있어야 함. 프로젝트 카드를 치고 들어온 거라. Jira는 좌측에 스페이스를 선택하면 선택된 스페이스 기준으로 움직임." | **[완료 — CR-018/019/020]** 진입감 IA를 3개 CR로 처리 완료: WS=슬랙식 격리 작업공간(WS스위처 LNB 상단·격리 멤버십), 3층 IA(전역/WS/프로젝트), LNB에 선택 WS 기준 프로젝트 나열+활성 표시(CR-018), 프로젝트 탭 구조·드래그·…메뉴(CR-019/020). 워크스페이스·프로젝트 두 층 모두 진입 컨텍스트 확보. **단 #15(전역 만들기 모달의 projectId 프리셋)는 별개로 남음** — 모달이 여전히 컨텍스트 미수신(흡수 표기 정정, 아래 #15 참조). (구 진단: LNB 정적·진입감 부재, IA 개정 필요.) |
| 15 | ✅ | 전역 [만들기] 업무 모달 / WMP-WI-001 | UX | M | "프로젝트를 선택해서 들어간 화면에서 만들기를 눌렀는데, 상단에서 프로젝트를 또 선택하도록 되어 있음" | **[수정완료 2026-06-28]** [create-modal.tsx](frontend/src/components/common/create-modal.tsx) — 모달이 현재 URL을 파싱(`/projects/:key/*`)해 프로젝트 컨텍스트 자동 추론. 프로젝트 화면에서 열면 그 프로젝트로 프리셋 + 프로젝트 Select 잠금(disabled). (CreateModal이 AppShell Outlet 바깥 마운트라 useParams 불가 → `useLocation().pathname` 정규식 파싱.) tsc+build PASS, 운영 배포 확인. (구 진단: store boolean만 보고 열려 projectId 항상 빈 값 시작.) |
| 14 | 🔧 | 프로젝트 만들기 1단계 템플릿 / WMP-WS-002 | UX+버그 | M | "프로젝트 만들 때 딱 정해진 3개만 있나요? 템플릿 생성은 어디서? 그냥 기본형은 없나요?" | **[부분해결 — CR-019/020, 2026-06-28 실측 정정]** ✅ **"기본형 없음" 해소**: DEFAULT 템플릿(id=4, 8탭 전부) 신설 — BE V5(`V5__basic_workflow_default_template.sql`)+FE 상수([domain.ts:191-192](frontend/src/types/domain.ts#L191)) 둘 다 추가, 4종으로 일치. ✅ **defaultTabs 불일치 버그 해소**: BE 시드↔FE 상수 손으로 맞춤(현재 일치 확인). ❌ **잔여 = FE BE조회 미일원화(구조 리스크, 버그 아님)**: [domain.ts:191](frontend/src/types/domain.ts#L191) 하드코딩 상수 + [api.ts:3](frontend/src/features/workspaces/api.ts#L3) "BE 엔드포인트 없음" 주석 그대로 → 두 곳 이중관리. 지금은 값 일치라 안 깨지나, 향후 한쪽만 바뀌면 재발. 템플릿 GET 엔드포인트 신설+상수 제거는 별도 과제(우선 L로 격하). ⚠️ 템플릿 사용자 CRUD는 여전히 없음(설계상 시드 마스터, Flyway로만 추가 — 의도된 것). **[원본 실측 보존]** ① **현재 3개 고정 맞음** — BE `project_template` 시드 DEV/OPS/PLAN 3행 `is_system=true`([V2__seed.sql:119](backend/src/main/resources/db/migration/V2__seed.sql#L119)). 단 FE 마법사는 BE를 안 부르고 **하드코딩 상수** `PROJECT_TEMPLATES`([domain.ts:186](frontend/src/types/domain.ts#L186)) 사용("BE 조회 엔드포인트 없음" [api.ts:3](frontend/src/features/workspaces/api.ts#L3) 주석). ② **템플릿 생성/관리 화면 없음** — 조회 GET 엔드포인트조차 없음(ProjectController의 templateId는 프로젝트 *필터*용일 뿐, template CRUD 0건). AdminTabs에 템플릿 탭 없음. 시드는 `is_system`이라 사용자 생성 미전제. ③ **"기본형" 없음** — DEV가 사실상 폴백(`templateCodeById` 미스 시 `'DEV'` 반환 [domain.ts:194](frontend/src/types/domain.ts#L194)), 빈/기본 템플릿 옵션 별도 없음. **⚠️ 함께 발견된 정합성 버그**: BE 시드 `default_tabs`와 FE 상수 `defaultTabs`가 **불일치**(예: DEV BE=`["backlog","board","timeline","list","issues"]` vs FE=`["summary","backlog","board","timeline","reports"]`). 두 곳을 손으로 따로 관리해 어긋남 → 마법사 표시 탭과 실제 생성 프로젝트 탭이 다를 위험. **수정 방향(정책 결정 필요)**: (A) 3종 고정 유지 + FE를 BE 조회로 일원화(템플릿 GET 엔드포인트 신설, 상수 제거, 정합성 버그 해소) / (B) 관리자 템플릿 CRUD 신규 기능(사용자 정의 템플릿 — 설계 범위 밖, CR 필요) / (C) "기본형(빈 템플릿)" 1종 추가. 최소한 (A)의 정합성 버그는 별도 처리 권장. 우선 M. |
| 13 | 🆕 | 프로젝트 만들기 마법사 3단계 "설정 확인" / WMP-WS-002 | UX | M | "여기는 설정할 수 있는 게 아무것도 없네요." | **[코드 실측됨]** 3단계에 사용자가 바꿀 수 있는 입력이 **0개**임이 맞음. ① 업무 유형([204-211줄](frontend/src/features/projects/components/ProjectCreateWizard.tsx#L204))=템플릿 `issueTypeCodes` **읽기전용 칩 표시만**(선택/해제 불가) ② 기본 보기(탭)([213-222줄](frontend/src/features/projects/components/ProjectCreateWizard.tsx#L213))=템플릿 `defaultTabs` **읽기전용 칩 표시만** ③ 샘플 업무 토글([223-228줄](frontend/src/features/projects/components/ProjectCreateWizard.tsx#L223))=`disabled`+`opacity-60`, "work_item(Sprint 3) 이후 지원" 문구인데 **Sprint 3은 이미 완료**(#7과 동일한 미연결 잔재). 단계명이 "설정 확인"인데 확인할 것도 설정할 것도 빈약 → 빈 화면 인상. **수정 방향(택1, 정책 결정 필요)**: (A) 단계를 **편집 가능**하게 — 유형·탭을 토글로 켜고 끄게(템플릿 기본값에서 가감) + 샘플 토글 활성화(BE 시드 연결) / (B) 단계를 **확인 전용으로 재구성** — 칩만 두지 말고 이름·키·기간·워크스페이스 요약까지 한 화면에 모아 "리뷰" 답게 / (C) **3단계 제거** — 4단계→3단계로 축소(유형·탭은 추후 프로젝트 설정에서 변경, 헤더 안내문 "나중에 변경 가능"과 일치). 일괄 수정 전 A/B/C 확정. 우선 M. |
| 12 | ✅ | 프로젝트 만들기 마법사 / WMP-WS-002 (2단계 기간) | 버그 | M | "달력이 생뚱맞게 기본 달력임. 다른 곳과 마찬가지로 컴포넌트 쓰게 해주세요. 다른 곳도 있으면 다 변경" | **[전 화면 실측됨]** 네이티브 브라우저 달력(`Input type="date"`)을 직접 쓰는 곳은 **ProjectCreateWizard 단 한 곳뿐** — 시작일/종료일 2필드 ([ProjectCreateWizard.tsx:191·194](frontend/src/features/projects/components/ProjectCreateWizard.tsx#L191)). CLAUDE.md "네이티브 `<input type=date>` 금지" 위반. **나머지 날짜 입력 화면은 전부 정상**(이미 ds-ui `DatePicker`): [DetailSidePanel:130·137](frontend/src/features/workitem/components/DetailSidePanel.tsx#L130)·[CreateSprintDialog:84·97](frontend/src/features/agile/components/CreateSprintDialog.tsx#L84)·[ProjectSettingsDialog:129·142](frontend/src/features/projects/components/ProjectSettingsDialog.tsx#L129). **수정 방향**: 마법사 2필드를 `Input type=date` → ds-ui `DatePicker`로 교체. 마법사 state가 string(`s.startDate`)이라 `@/lib/date`의 `fromIso(value)`/`toIso(onChange d)` 변환으로 끼움(DetailSidePanel과 동일 패턴). import에 `DatePicker` 추가. **변경 대상은 이 한 화면 2필드로 한정 — 다른 곳은 손댈 필요 없음.** 우선 M. |

---

## 일괄 수정 로그
> "수정하자" 지시 후, 위 항목을 묶어 처리한 기록. (수정 커밋·범위·검증 결과)

### 2026-06-27 (2회차) — 기능 버그 2종 수정 (#20·#12) — 디자인 이슈는 미룸
- **선별**: 누적 20건 중 디자인(A그룹)·별도트랙(C그룹)은 미루고, 디자인 아닌 **명백한 기능 버그 2건만** 우선 처리(사용자 결정).
- **#20 프로젝트 노출 탭 미반영 (H, 한 줄)**: [ProjectLayout.tsx:17-21](frontend/src/layout/ProjectLayout.tsx#L17) — 탭을 템플릿 프리셋 고정에서 `project.activeTabs?.length ? project.activeTabs : PROJECT_TAB_PRESET[...]`(저장값 우선·프리셋 폴백)으로 변경. 설정에서 켠 탭이 화면에 반영됨. 설계·BE 변경 없음.
- **#12 마법사 네이티브 달력 → ds-ui DatePicker**: [ProjectCreateWizard.tsx](frontend/src/features/projects/components/ProjectCreateWizard.tsx) 2단계 시작일/종료일 `Input type=date` → `DatePicker`(import에 DatePicker 추가·`Input`은 이름/키에서 계속 사용하므로 유지, `@/lib/date` fromIso/toIso로 string↔Date). **#19 교훈 선반영**: DatePicker에 `className="w-full"` 부여해 잘림 예방. CLAUDE.md 네이티브 date 금지 규칙 충족.
- **검증**: `tsc -b` PASS(exit 0) · `vite build` 통과(`index-Njqv4dSC.js`). 규모=소규모(설계 변경 없음, 기존 BE·설계 내 버그픽스). **운영 배포·화면 E2E는 별도(미실행).**
- **남은 디자인/구조 항목**: A그룹(#1·2·3·4·5+19·8)·C그룹(#9·10·11·17)은 디자인/배포/ds-ui백로그로 보류. B그룹(#6·13·14·16·18)은 정책 결정 후 처리. #16(진입감) = H, 규모 판단 필요.

### 2026-06-27 — FE 미연결 4종 연결 (#7 포함)
- **범위**: BE 엔드포인트는 있으나 FE 진입점·호출이 없던 4종을 연결(신규 BE 없음, BE 계약 코드 실측 후 맞춤).
  - **WMP-WI-001 업무 생성**: create-modal stub → 실제 생성 폼(`POST /work-items`). #7 해소.
  - **WMP-WS-001 워크스페이스 생성/수정**: `POST·PATCH /workspaces` + ProjectsPage 헤더/필터바 진입(Admin·Owner).
  - **WMP-WS-004 프로젝트 수정/보관**: `PATCH /projects/{id}`·`/archive` + ProjectSettingsDialog(Manager 이상).
  - **WMP-WS-006 가시성 변경**: `PATCH /projects/{id}/visibility` + 설정 다이얼로그 내 공개/비공개 토글.
- **진입점**: 전역 [만들기] 모달 / ProjectsPage 헤더·워크스페이스 수정줄 / ProjectCard 우상단 메뉴 / SummaryView 헤더 설정 버튼.
- **검증**: `tsc -b` + `vite build`(3400 modules) 통과 · 4종 api 계약 테스트 8/8 PASS(node 환경 fetch 스텁). 운영 화면 E2E는 별도(배포 후).
- **UI 규칙 준수**: ds-ui만 사용(네이티브 alert/confirm/select/date 없음). 생성/저장=primary, 취소=ghost, 보관(파괴)=destructive+공통 ConfirmDialog.
