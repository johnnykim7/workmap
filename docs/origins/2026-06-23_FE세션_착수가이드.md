# Sprint 1 FE 세션 — 착수 가이드

> 작성: 2026-06-23 | BE 세션 종료 직후. 새 FE 세션이 이 대화 맥락 없이 바로 착수하기 위한 인계.
> 읽기 순서: CLAUDE.md → **이 파일** → docs/execution-spec.md §5 Sprint1(FE) → docs/T3-3_화면_구조.md(글로벌 LNB §9.1).

---

## 0. 한 줄 상황

**Sprint 1 BE 세션은 완료·커밋됨**(`feat/SPR-01-setup` 브랜치, 커밋 2개). 다음 = **Sprint 1 FE 골격 구현**. BE가 떠 있고 health 200·Flyway 적용 완료.

---

## 1. BE 세션이 끝낸 것 (FE가 붙을 대상)

- `backend/` 신규: Spring Boot 3.4.2 + Java 17 + MyBatis + PostgreSQL16 + Flyway + bp-common-lib 0.1.0
- **BE 포트 8186** (전역 포트 레지스트리 등록 완료). 로컬 실행: `cd backend && JAVA_HOME=$(/usr/libexec/java_home -v 17) ./gradlew bootRun`
- DB = **운영 PG16의 workmap DB**. 로컬에서 붙으려면 SSH 터널 필요:
  `ssh -i ~/.ssh/id_ed25519 -N -L 15432:localhost:5432 therecommerce@59.8.160.12` (application.yml 기본 DB_URL이 localhost:15432). 상세 메모리 [[workmap-db-server]].
- 시드 완료: issue_type 5 / workflow 3종(개발·운영·현장검증)+상태16+전이28 / measure_unit 7 / project_template 3(DEV·OPS·PLAN) / field_scheme 11
- ⚠️ Sprint 1 BE는 **세팅까지만**. 인증/프로젝트/work_item API는 **아직 없음**(Sprint 2~3). FE 골격 단계에선 API 호출 화면까지 안 가도 됨 — 라우팅·LNB·Query/Zustand·ds-ui 셋업이 목표.

---

## 2. 확정 결정 (이 세션에서 정함)

1. **frontend/ 재사용** — 기존 목업 frontend/를 v0.4 실구현으로 발전시킨다(별도 frontend-v2 안 만듦). 목업 컴포넌트(src/, src.clickup-backup/)는 참고용으로 두고 v0.4 골격을 얹는다.
2. **Node 22 사용** — 설치된 v22.21.1. 기본 v18은 corepack/pnpm 서명 에러 + Vite/React19 비호환. 세션 시작 시 `nvm use 22` 후 작업. `.nvmrc`에 22 고정 권장.
3. **포트 FE 3186** (BE 8186 - 5000, 전역 레지스트리). 현재 목업 dev 서버가 3186 점유 중일 수 있음 → 끄고 실구현으로 교체.

---

## 3. 착수 전 블로커 해소 (순서대로)

1. **Node 전환**: `nvm use 22` (또는 `.nvmrc` 22 생성 후). pnpm은 Node 22의 corepack으로 정상 동작.
2. **3186 목업 종료**: 기존 목업 dev 서버가 돌고 있으면 종료(`lsof -nP -iTCP:3186 -sTCP:LISTEN`로 PID 확인 후 kill).
3. **.gitignore**: 현재 `/frontend/` 통째 제외 중(목업이라 제외했던 것). **FE 실구현을 git 추적하려면 이 라인을 풀고** node_modules/dist만 제외하도록 수정해야 함. (frontend/.gitignore 또는 루트에서 조정)

---

## 4. FE 골격 목표 (execution-spec §5 Sprint1 FE)

- Vite + React19 + TS + ds-ui 설치, React Router v6 / TanStack Query / Zustand 골격
- **글로벌 LNB**(T3-3 §9.1) — 차분한 톤(CLAUDE.md FE 규칙: 색 절제, ds-ui 필수, 네이티브 UI 금지)
- 검증: `pnpm dev`(3186) + `pnpm build` 성공
- **추가 설치 필요**: react-hook-form + zod (목업 package.json에 없음 — CLAUDE.md 폼 검증 필수)

## 5. 목업 스택 현황 (frontend/package.json — 이미 v0.4와 거의 일치)

이미 있음: React19 / Vite6 / @tanstack/react-query / zustand / react-router-dom@6 / @therecommerce/ds-ui(file: 로컬링크) / tailwindcss@4 / lucide-react.
없음(추가): **react-hook-form, zod**.
ds-ui는 `file:../../../bp-platform/design-system/packages/ui` 로컬 링크 — 설치 함정은 메모리 [[workmap-mockup-stack]] 참조.

---

## 6. 주의

- CLAUDE.md FE 아키텍처 규칙 엄수: ds-ui 필수(alert/confirm/네이티브 select·date·dialog 금지), 색 절제(신호색만), 버튼 variant 일관, 로딩=스켈레톤, 공통 컴포넌트 재사용.
- 자체 composite 제작 대상만: WorkItemCard/KanbanBoard/BacklogRow/SprintHeader/StatusBadge/TypeBadge/MeasureBar/SplitView/CreateModal/ProjectCreateWizard.
- 코드 수정 전 규모 판단 게이트(훅) 발동 — 이번 세션은 이미 "대규모(설계 완료)"로 판단됨.
