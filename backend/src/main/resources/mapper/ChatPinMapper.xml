<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.therecommerce.workmap.chat.mapper.ChatPinMapper">

    <resultMap id="pinMap" type="com.therecommerce.workmap.chat.domain.ChatPin">
        <id     property="id"        column="id"/>
        <result property="channelId" column="channel_id"/>
        <result property="messageId" column="message_id"/>
        <result property="pinnedBy"  column="pinned_by"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.therecommerce.workmap.chat.domain.ChatPin"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO chat_pins (channel_id, message_id, pinned_by)
        VALUES (#{channelId}, #{messageId}, #{pinnedBy})
    </insert>

    <select id="findByChannelAndMessage" resultMap="pinMap">
        SELECT id, channel_id, message_id, pinned_by, created_at
        FROM chat_pins
        WHERE channel_id = #{channelId} AND message_id = #{messageId}
    </select>

    <select id="findByChannel" resultMap="pinMap">
        SELECT id, channel_id, message_id, pinned_by, created_at
        FROM chat_pins
        WHERE channel_id = #{channelId}
        ORDER BY created_at DESC, id DESC
    </select>

    <delete id="delete">
        DELETE FROM chat_pins
        WHERE channel_id = #{channelId} AND message_id = #{messageId}
    </delete>

</mapper>
