<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.therecommerce.workmap.agile.mapper.SprintMapper">

    <resultMap id="sprintMap" type="com.therecommerce.workmap.agile.domain.Sprint">
        <id     property="id"          column="id"/>
        <result property="projectId"   column="project_id"/>
        <result property="name"        column="name"/>
        <result property="goal"        column="goal"/>
        <result property="status"      column="status"/>
        <result property="startDate"   column="start_date"/>
        <result property="endDate"     column="end_date"/>
        <result property="sortOrder"   column="sort_order"/>
        <result property="startedAt"   column="started_at"/>
        <result property="completedAt" column="completed_at"/>
        <result property="createdAt"   column="created_at"/>
        <result property="updatedAt"   column="updated_at"/>
    </resultMap>

    <insert id="insert" parameterType="com.therecommerce.workmap.agile.domain.Sprint"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sprints (project_id, name, goal, status, start_date, end_date, sort_order)
        VALUES (#{projectId}, #{name}, #{goal},
                COALESCE(#{status}, 'FUTURE'), #{startDate}, #{endDate},
                COALESCE(#{sortOrder}, 0))
    </insert>

    <select id="findById" resultMap="sprintMap">
        SELECT * FROM sprints WHERE id = #{id}
    </select>

    <select id="findByProject" resultMap="sprintMap">
        SELECT * FROM sprints
        WHERE project_id = #{projectId}
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="findActiveByProject" resultMap="sprintMap">
        SELECT * FROM sprints
        WHERE project_id = #{projectId} AND status = 'ACTIVE'
        ORDER BY id ASC
        LIMIT 1
    </select>

    <!-- CR-039: 병렬 스프린트 보드 그룹용 — 프로젝트의 ACTIVE 스프린트 전부(id 오름차순). -->
    <select id="findAllActiveByProject" resultMap="sprintMap">
        SELECT * FROM sprints
        WHERE project_id = #{projectId} AND status = 'ACTIVE'
        ORDER BY id ASC
    </select>

    <select id="findAllActive" resultMap="sprintMap">
        SELECT * FROM sprints
        WHERE status = 'ACTIVE'
        ORDER BY id ASC
    </select>

    <update id="updateStart" parameterType="com.therecommerce.workmap.agile.domain.Sprint">
        UPDATE sprints
        SET status     = 'ACTIVE',
            start_date = #{startDate},
            end_date   = #{endDate},
            started_at = #{startedAt},
            updated_at = now()
        WHERE id = #{id}
    </update>

    <update id="updateComplete">
        UPDATE sprints
        SET status       = 'COMPLETED',
            completed_at = #{completedAt},
            updated_at   = now()
        WHERE id = #{id}
    </update>

    <select id="maxSortOrder" resultType="java.lang.Integer">
        SELECT MAX(sort_order) FROM sprints WHERE project_id = #{projectId}
    </select>

    <!-- 편집(CR-038): name/goal/기간만 수정, status 무변경 -->
    <update id="updateEdit" parameterType="com.therecommerce.workmap.agile.domain.Sprint">
        UPDATE sprints
        SET name       = #{name},
            goal       = #{goal},
            start_date = #{startDate},
            end_date   = #{endDate},
            updated_at = now()
        WHERE id = #{id}
    </update>

    <!-- 삭제(CR-038): FUTURE 스프린트 하드 삭제(서비스에서 상태 가드·항목 백로그 복귀 선행) -->
    <delete id="deleteById">
        DELETE FROM sprints WHERE id = #{id}
    </delete>

</mapper>
