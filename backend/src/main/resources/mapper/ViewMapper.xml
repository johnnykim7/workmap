<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.therecommerce.workmap.view.mapper.ViewMapper">

    <!-- TimelineItem 공통 컬럼(work_item 파생 뷰, BIZ-106) -->
    <sql id="itemCols">
        id            AS id,
        key           AS key,
        title         AS title,
        issue_type    AS issueType,
        common_status AS commonStatus,
        priority      AS priority,
        assignee_id   AS assigneeId,
        epic_id       AS epicId,
        start_date    AS startDate,
        due_date      AS dueDate,
        progress      AS progress
    </sql>

    <!-- 타임라인: 시작일 또는 기한이 있는 항목만(막대 그릴 수 있는 것) -->
    <select id="timeline" resultType="com.therecommerce.workmap.view.dto.ViewDtos$TimelineItem">
        SELECT <include refid="itemCols"/>
        FROM work_items
        WHERE project_id = #{projectId}
          AND deleted_at IS NULL
          AND (start_date IS NOT NULL OR due_date IS NOT NULL)
        ORDER BY COALESCE(start_date, due_date) ASC, due_date ASC, id ASC
    </select>

    <!-- 캘린더: 기한이 지정 월 범위[from, to]에 드는 항목 -->
    <select id="calendar" resultType="com.therecommerce.workmap.view.dto.ViewDtos$TimelineItem">
        SELECT <include refid="itemCols"/>
        FROM work_items
        WHERE project_id = #{projectId}
          AND deleted_at IS NULL
          AND due_date IS NOT NULL
          AND due_date BETWEEN #{from} AND #{to}
        ORDER BY due_date ASC, id ASC
    </select>

</mapper>
